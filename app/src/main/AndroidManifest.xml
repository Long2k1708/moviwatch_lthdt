<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.princefrog2k.lthdt.moviwatch">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MoviWatch"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".activities.WatchMovieActivity"
            android:label="@string/title_activity_watch_movie"
            android:theme="@style/Theme.MoviWatch.NoActionBar"></activity>
        <activity
            android:name=".activities.MovieListActivity"
            android:label="@string/title_activity_movie_list"
            android:theme="@style/Theme.MoviWatch.NoActionBar" />
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>